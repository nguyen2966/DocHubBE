export enum WorkspaceRole {
  ADMIN = 'admin',
  MEMBER = 'member',
}

export enum InvitationAction {
  INVALID = 'invalid',
  SIGN_UP = 'sign-up',
  SIGN_IN = 'sign-in',
  VERIFY_REQUIRED = 'verify-required',
  ACCEPTED = 'accepted',
}
